# G4 Frontend Packet 03 — 11:50 Reconciliation

Status: `PACKET_03_REMAINS_ACTIVE / A_PREDECESSOR_REQUIRED / RESEARCH_ONLY`
Date: 2026-09-22
Lane: `G4 Frontend Planning :50`

## Decision

Do **not** create Packet 04 yet.

The current round produced material semantic refinement, but Construction A has not yet materialized the five Packet 03 lower/middle contracts and Construction B is correctly waiting for that predecessor. Advancing to ModuleNode/ModuleWorkbox, module-internal radial anatomy, Door/Counter/Corridor/Handoff/Gate composition, Tool, Workspace or Main Canvas would violate the bottom-up progression.

Packet 03 remains the bounded research packet for the next A/B cycle.

## Predecessors consumed

### Construction A :00

Latest A evidence remains the blocked reconciliation predating executable Packet 03 output. No materialized contracts yet exist for:

- `ProjectionIdentityRef`;
- `SelectionBridge`;
- `QualifiedRelationPort`;
- `FloorManifestationPort`;
- `CriticalStateMarker`.

Therefore A is not semantically contradicted; it is simply the missing predecessor required for promotion.

### Construction B :10

`G4_WORKSPACE_COMMAND_SURFACE_CONSTRUCTION_B.md` correctly reconciles against Packet 03 and narrows its successor work to exactly:

- `ModuleProjectionTile`;
- `ProjectionHandoff`.

B remains blocked until A materializes the five Packet 03 contracts. The former missing-Planning blocker is resolved; the remaining blocker is sequencing only.

### G4 Research Consolidation :20

`G4_MAIN_COMPOSITION_CANVAS_3D_SEMANTIC_MODEL_RESEARCH.md` adds implementation-independent semantics for the building/floor/onion model:

- `ModuleFace != ModuleIdentity`;
- capability participation is transversal and does not create ownership;
- Door is a typed admissible boundary/port;
- Counter is a semantic request/service entry surface, not a form;
- Corridor is typed designed progression, not a generic edge;
- Handoff is explicit responsibility/control/context transfer;
- Gate verifies a prerequisite and does not manufacture it;
- Evidence supports qualified claims and does not own business truth;
- DESIGNED, OBSERVED and ASSESSED remain distinct;
- downstream observation cannot backfill upstream passage/evidence.

These findings are accepted as successor constraints, but they do not widen Packet 03.

### G4 Frontend Research :40

`G4_MAIN_COMPOSITION_CANVAS_FLOOR_PROJECTION_IDENTITY_RESEARCH.md` materially refines the projection substrate:

- floor = typed projection stratum, not canonical container;
- floor != bounded context, owner, deployment boundary or permission boundary;
- candidate floors are question-oriented rather than one mandatory universal stack;
- identity separates `CanonicalSemanticIdentity`, `ManifestationIdentity`, `ProjectionIdentity` and `RenderInstanceId`;
- cross-floor relations require explicit disposition: `SAME_IDENTITY_PROJECTION`, `MANIFESTATION_OF`, `DERIVED_FROM`, `OBSERVED_INSTANCE_OF`, `RELATED_NOT_IDENTITY`, `UNQUALIFIED`, `INCOMPATIBLE`;
- capability shaft is a relation/navigation projection, not an identity assertion;
- vertical `+` must qualify target floor and relation kind before any governed creation/binding;
- designed and observed identity remain asymmetric;
- cross-projection selection preserves semantic identity even when no direct target representation exists;
- zoom, floor, lens, camera and selection are independent dimensions.

These are material refinements to the proof obligations of Packet 03, not grounds to skip it.

## Reconciled deltas applied to Packet 03 execution

Construction A must now treat the following as mandatory refinements while executing the existing five objects.

### ProjectionIdentityRef

Must distinguish at least:

```text
CanonicalSemanticIdentity
ManifestationIdentity?
ProjectionIdentity
RenderInstanceId
```

Renderer-local identity terminates at the picking/render adapter.

Required invariant:

`RenderInstanceId != ProjectionIdentity != canonical semantic identity`.

### SelectionBridge

Must preserve semantic selection across floor/projection changes with explicit outcomes such as:

```text
PRESERVED_EXACT
PRESERVED_SEMANTIC_REPROJECTED
PRESERVED_WITH_MANIFESTATION_SUMMARY
NOT_MATERIALIZED_BUT_RETAINED
FILTERED_OUT_BUT_RETAINED
NO_LONGER_DISCLOSED
NO_LONGER_ADMISSIBLE
STALE_REFERENCE
AMBIGUOUS_REPROJECTION
```

Changing camera, zoom, lens, floor or representation must not silently mutate selection identity.

### FloorManifestationPort

The previous same-vs-derived distinction is refined. A must test at least:

```text
SAME_IDENTITY_PROJECTION
MANIFESTATION_OF
DERIVED_FROM
OBSERVED_INSTANCE_OF
RELATED_NOT_IDENTITY
UNQUALIFIED
INCOMPATIBLE
```

`Vertical + != clone`.
`Projection creation != canonical object creation`.
`Same X/Z position across floors != same identity`.

### QualifiedRelationPort

Qualification must remain independent of visual adjacency and must preserve contract/currentness/authority evidence. A relation visible in one projection is not automatically admissible, compatible or creatable.

### CriticalStateMarker

Representation floors must also preserve cross-floor/shaft discontinuities including at least:

```text
UNKNOWN
STALE
PARTIAL
DRIFTED
CONFLICTED
BLOCKED_BY_DISCLOSURE
RECONCILING
```

A visually continuous shaft must not imply semantic/evidence continuity.

## Deferred but now recorded successor topic — module internal anatomy

A material product hypothesis has emerged for the packet **after** projection primitives/patterns are qualified: each module participates in global floors but also has an internal compositional anatomy analogous to a datacenter rack or bounded semantic cell.

Candidate internal concerns include:

```text
Core / capability intent
Contracts / ports
Services
Workflow
Data flow
Policies / authority
Providers / bindings
Adapters / drivers
Runtime / deployment hooks
Observability / evidence
```

This should be researched as **module-internal layers/radials**, not confused with global floors.

Provisional invariants for later planning:

- `Global floor != module-internal layer`;
- `Module internal face != new ModuleIdentity`;
- `Internal radial position != ownership/authority`;
- one module may project internal concerns onto several global floors;
- internal contracts/workflow/data-flow/services are compositional anatomy, not automatically separate modules;
- nested/fractal visualization must preserve canonical identity and currentness rather than recursively cloning objects.

No Module Internal Anatomy packet is authorized yet. Planning may promote it only after Packet 03 A/B evidence establishes projection identity and handoff semantics.

## A handoff — next exact work

Construction A :00 must execute Packet 03 now, incorporating the refinements above, and materialize only:

1. `ProjectionIdentityRef`;
2. `SelectionBridge`;
3. `QualifiedRelationPort`;
4. `FloorManifestationPort`;
5. `CriticalStateMarker`.

A must produce state/transition/evidence/accessibility/performance contracts and return unresolved semantic decisions to Planning.

A must not create ModuleNode, ModuleWorkbox, module-internal anatomy, Door/Counter/Corridor/Handoff/Gate family, Tool, Workspace or Main Canvas.

## B handoff — unchanged sequencing

After fresh A output, Construction B :10 may integrate exactly:

1. `ModuleProjectionTile`;
2. `ProjectionHandoff`.

B must test cross-projection identity, semantic zoom/aggregation, stale/currentness invalidation, relation/floor eligibility changes, keyboard/non-drag equivalence, UNKNOWN/PARTIAL effects and critical-state preservation.

B must not promote complete Main Canvas yet.

## Proof obligations added this round

- floor membership/projection never creates canonical containment or ownership;
- same geometry across floors never proves same identity;
- same semantic identity may have multiple ProjectionIdentity values;
- one semantic identity may relate to multiple manifestations;
- shaft continuity never implies evidence/currentness continuity;
- selection can survive when target projection cannot materialize the object directly;
- camera, zoom, lens, floor and selection are independent state dimensions;
- observed runtime instance never becomes designed identity by visual alignment;
- LOD/aggregation cannot erase identity class, critical uncertainty or disclosure boundary;
- global floor semantics and future module-internal radial/layer semantics remain distinct.

## Componentes impact

No new higher-level inventory entry is promoted in this reconciliation. Packet 03 still requires entries/scenarios for its five objects. Add scenario coverage for:

- canonical vs manifestation vs projection vs render identity;
- cross-floor same-identity projection;
- manifestation-of and observed-instance-of;
- unqualified/incompatible vertical target;
- shaft with stale/unknown intermediate stop;
- selection retained with no direct target representation;
- camera/zoom/lens change without selection mutation.

The future Module Internal Anatomy inventory is deferred until its predecessor gate closes.

## Closure / blocker / maturity

Current blocker:

`BLOCKER-G4-FE-PL-004 = Packet 03 Construction A materialization absent`.

This is a sequencing blocker, not a semantic contradiction and not a reason to stop the recurring research lane.

Maturity:

`PACKET_03_ACTIVE / SEMANTICS_REFINED / A_REQUIRED / B_WAITING`.

Packet 04 must not be created until fresh A/B evidence either closes Packet 03 or identifies a material lower-level gap that requires bounded repair.
