# G4 Frontend Construction A — bounded reconciliation

Date: 2026-09-22
Status: `BLOCKED_BY_MISSING_PLANNING_PACKET / NON_EXECUTABLE`
Scope: `G4 Frontend Design System & UI Foundation`

## Purpose

Record the bounded Construction A reconciliation required while the latest materialized `G4 Frontend Planning :50` packet is absent. This artifact does **not** create successor scope and does **not** authorize implementation.

## Repository truth revalidated

Construction A re-read/revalidated the governing frontend research context on branch `research/g4-product-rnd-foundations`, including the G4 frontend foundation plan and the current reconciliation state. G4 remains research-only/non-executable for product implementation.

Repository search on 2026-09-22 again found no materialized bounded packet identifiable as the latest `G4 Frontend Planning :50` packet. Therefore Construction A must not select or promote a successor slice independently.

## Direction now awaiting Planning materialization

The recurring lane now has an explicit candidate inventory for the future Main Composition Canvas research. This is recorded only as **pending Planning input**, not as executed Construction A scope:

```text
ModuleNode
ModuleWorkbox
ModuleFace
HorizontalRelationPort
VerticalFloorPort
FloorManifestation
CapabilityShaft
Door/Port
Counter/EntryPoint
CorridorSegment
CorridorJunction
Gate/Checkpoint
HandoffMarker
WorkItemMarker
DeviationMarker
SelectionIndicator
CameraModeControl
LOD/AggregationRepresentation
Ribbon/ContextualTab commands
2D/list fallbacks
```

The direction also preserves the emerging fractal/module-anatomy hypothesis: a module may participate in system-level floors while exposing its own internal work faces/layers/radials (for example contracts, services, workflow, data flow, adapters/providers, runtime/observability). Planning must decide which bounded subset is next; Construction A does not promote the whole inventory at once.

## Preserved contracts

The following are treated as already-decided constraints for the future packet, not new implementation authority:

- `+ horizontal` = qualified relation/composition candidate, never arbitrary add-anything;
- `+ vertical` = manifestation of the same module in another sphere/floor;
- `Door` = admissible boundary;
- `Counter` = operation/request surface exposed at that boundary;
- `Corridor` = allowed/expected work path, not a generic graph edge;
- `Handoff` = transfer occurrence, not the whole corridor;
- `Gate` must be able to represent `PASS`, `FAIL`, `BLOCKED`, `PENDING`, `UNKNOWN`, `STALE`, `BYPASSED_WITH_AUTHORITY` when the eventual packet selects it;
- `capability participation != semantic ownership`;
- `selected != focused`;
- `expanded != opened-in-workspace`;
- `ACK != effect`;
- `designed != observed`;
- renderer representation must not become semantic authority;
- 3D, 2D and textual projections must preserve semantic identity where applicable.

## Performance/accessibility constraints awaiting packet binding

When Planning selects Canvas primitives, Construction A must bind their contracts without coupling semantics to renderer mechanics. Candidate performance techniques such as instancing, picking optimization, LOD, label virtualization, culling, clustering and render-on-demand may alter representation cost/detail, but must not silently remove semantic state, gates, evidence or required actions.

The eventual packet must preserve a non-3D route for core operations where applicable, keyboard-reachable interaction, non-color state semantics, reduced-motion equivalents and non-drag alternatives.

## Required Planning packet contract

For Construction A to execute without redefining scope, the next `G4 Frontend Planning :50` packet must materialize at minimum:

1. packet identity/date and predecessor evidence;
2. a **bounded named subset** of the candidate inventory above;
3. composition levels in scope (`TOKEN -> PRIMITIVE -> COMPONENT -> PATTERN/BLOCK -> DOMAIN BUILDING BLOCK`);
4. explicit exclusions/stop conditions;
5. required interaction, authority/availability, async, content/currentness, validation/editing and operational state families per selected element;
6. valid/invalid transition proof obligations and recovery;
7. applicable adversarials, including semantic incompatibility despite visual connectability, focus/selection confusion, duplicate activation, stale-as-current, partial-as-success, LOD semantic loss, 3D-only action and counter/form-contract conflation;
8. evidence targets/classes;
9. `Componentes` inventory impact;
10. Construction B integration obligations;
11. unresolved decisions that return to Planning.

## Construction A acceptance shape once unblocked

For each named element Construction A will materialize, when applicable:

```text
purpose
semantic identity
inputs / outputs / events
variants != states
pointer / keyboard / focus / selection semantics
enabled / disabled / read-only / permission states
async states
content / empty / partial / stale / unknown / error states
validation / editing states
guards
valid transitions
invalid transitions
failure / recovery
reduced motion
accessibility
responsive behavior
token / icon dependencies
composition rules
composedOf
usedBy candidates
lifecycle status
Componentes inventory metadata
test / scenario candidates
```

For selected Counter/EntryPoint work, the packet should additionally bind request/command/query/event/handoff contracts, required/optional data, actor/authority, preconditions, validation/evidence, accepted/rejected/needs-information/blocked outcomes, timeout/retry/escalation and acknowledgement-vs-effective-completion semantics.

Evidence remains classified as `EVIDENCE`, `HYPOTHESIS` or `CANDIDATE`; provider/library behavior must not silently become System Builder semantic truth.

## State-contract delta

No state contract is changed by this reconciliation. No candidate primitive is promoted to an accepted component contract. The new Canvas inventory is recorded only to eliminate ambiguity for Planning.

## Componentes impact

No lifecycle promotion in this round. Planning should explicitly nominate which Canvas elements enter `Componentes` as research candidates in the next bounded packet, including required 3D/2D/text representations and state matrices.

## Construction B handoff

`BLOCKED` pending the Planning packet. Exact handoff remains:

```text
Planning :50 materializes bounded Canvas subset
  -> Construction A executes named lower/middle primitives/contracts
  -> Construction A records evidence + state/transition/composition contracts
  -> Construction B integrates those contracts into workspace/tool/task flows
     without arbitrary redefinition
```

## Blocker

`BLOCKER-G4-FE-CA-001`: latest materialized `G4 Frontend Planning :50` bounded packet still not found on `research/g4-product-rnd-foundations` after revalidation on 2026-09-22.

Resolution owner: `G4 Frontend Planning :50`.

Construction A remains ready to execute immediately after that packet is committed.
