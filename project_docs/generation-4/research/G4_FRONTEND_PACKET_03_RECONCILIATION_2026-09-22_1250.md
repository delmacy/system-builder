# G4 Frontend Packet 03 — 12:50 Reconciliation

Status: `PACKET_03_REMAINS_ACTIVE / A_PREDECESSOR_REQUIRED / CARDINALITY_REFINED / RESEARCH_ONLY`
Date: 2026-09-22
Lane: `G4 Frontend Planning :50`

## Decision

Do **not** create Packet 04 yet.

The round produced a material frontend delta in manifestation cardinality, aggregation and ambiguity, but repository truth still lacks fresh Construction A materialization of the five Packet 03 lower/middle contracts. Construction B is therefore correctly waiting. Promotion to ModuleNode/ModuleWorkbox, module-internal radial anatomy, Door/Counter/Corridor/Handoff/Gate composition, Tool, Workspace or complete Main Canvas would still violate bottom-up progression.

Packet 03 remains the bounded packet. This reconciliation narrows new cardinality findings into additional proof obligations for the existing five objects rather than widening scope.

## Predecessors consumed

### Construction A :00

Latest materialized A artifact remains `G4_FRONTEND_CONSTRUCTION_A_RECONCILIATION_2026-09-22.md`, which predates Packet 03 execution and contains no bounded contracts for:

- `ProjectionIdentityRef`;
- `SelectionBridge`;
- `QualifiedRelationPort`;
- `FloorManifestationPort`;
- `CriticalStateMarker`.

The old missing-Planning condition is stale because Packet 03 now exists; the actual remaining predecessor is fresh A execution of Packet 03.

### Construction B :10

`G4_WORKSPACE_COMMAND_SURFACE_CONSTRUCTION_B.md` remains correctly bounded to two successor patterns after A:

- `ModuleProjectionTile`;
- `ProjectionHandoff`.

B explicitly refuses to promote a complete Canvas while A is absent. No change to B scope is authorized by this reconciliation.

### G4 Research Consolidation :20

The current semantic model preserves the building/floor/onion metaphor only where geometry has typed meaning. It keeps projection distinct from canonical truth, visual adjacency distinct from semantic relation, and designed path distinct from observed path. Door/Counter/Corridor/Handoff/Gate semantics remain successor constraints, not Packet 03 scope.

### G4 Frontend Research :40

`G4_MAIN_COMPOSITION_CANVAS_MANIFESTATION_CARDINALITY_RESEARCH.md` adds the material delta of this round:

- cardinality belongs to a typed relation contract, never geometry;
- `ONE_TO_ONE`, `ONE_TO_ZERO_OR_ONE`, `ONE_TO_MANY`, `MANY_TO_ONE`, `MANY_TO_MANY`, `UNKNOWN_CARDINALITY` and `CONTEXT_DEPENDENT` are candidate research dispositions;
- one designed identity may map to many observed instances without design duplication;
- many capabilities may map to one deployment/packaging manifestation without ownership transfer;
- one logical identity may have many placement/deployment manifestations;
- observed-to-designed mapping may remain ambiguous and must not auto-resolve by proximity/ranking;
- shafts may branch/aggregate without manufacturing identity;
- vertical `+` must qualify relation cardinality and existing admissible targets before implying creation;
- conformance is multidimensional and cannot be reduced to count equality;
- performance envelopes must distinguish logical object count, manifestation count and rendered primitive count.

These findings materially refine Packet 03, especially `FloorManifestationPort`, `SelectionBridge`, `ProjectionIdentityRef` and `CriticalStateMarker`.

## Reconciled material delta

### 1. ProjectionIdentityRef — cardinality scope

A must now prove that identity references can participate in typed relations whose cardinality is scoped and qualified without embedding cardinality into identity itself.

Required distinctions:

```text
CanonicalSemanticIdentity
ManifestationIdentity?
ProjectionIdentity
RenderInstanceId
RelationIdentity / relation context where needed
CardinalityScope where needed
```

Invariants:

- `Identity != cardinality`;
- `Visual alignment != one-to-one identity`;
- `One deployment manifestation != one capability`;
- `One capability != one observed runtime instance`;
- `Observed multiplicity != designed duplication`;
- `Unknown cardinality != cardinality one`.

Renderer-local `instanceId` still terminates at the render/picking adapter.

### 2. SelectionBridge — semantic anchor vs manifestation/aggregate selection

A must qualify selection without collapsing logical anchor, manifestation and aggregate.

Candidate shape to research:

```text
semanticAnchor
primaryManifestation?
aggregateSelection?
projectionIdentity
revision/currentness
```

Required cases:

- select logical identity with N manifestations;
- select one manifestation while retaining the logical semantic anchor;
- select aggregate without silently selecting first member;
- move from aggregate 3D representation to list/table members while preserving anchor;
- disclosure prevents enumeration, so a qualified aggregate survives without fabricated hidden identities;
- re-instancing changes renderer instance IDs while selection remains stable.

`Aggregate selection != member selection`.
`Selecting manifestation != implicit semantic pivot`.

### 3. QualifiedRelationPort — cardinality is part of qualification

Horizontal relation eligibility must include relation-kind/cardinality/currentness/authority qualification where material.

A must test at least:

```text
RELATION_KIND_UNKNOWN
CARDINALITY_UNKNOWN
CARDINALITY_COMPATIBLE
CARDINALITY_CONFLICT
EXISTING_TARGET_SATISFIES_MAX
ELIGIBLE
INELIGIBLE(reason)
UNKNOWN
```

A visible `+` never means another target is admissible.

### 4. FloorManifestationPort — existing targets and multiplicity

This is the most materially affected Packet 03 object.

Required transition refinement:

```text
SOURCE_SELECTED
-> TARGET_FLOOR_SELECTED
-> RELATION_KIND_QUALIFYING
-> CARDINALITY_QUALIFYING
-> EXISTING_TARGETS_DISCOVERING
-> NAVIGATE_EXISTING
 | PROPOSE_NEW_MANIFESTATION
 | PROPOSE_BINDING
 | OPEN_RELATION_INSPECTOR
-> authority/compatibility/currentness qualification
-> SUBMITTED
-> ACCEPTED | REJECTED | UNKNOWN_OUTCOME
-> EFFECTIVE | PARTIAL | FAILED | RECONCILING
```

Required proof cases:

- `ONE_TO_ZERO_OR_ONE` with existing admissible target blocks misleading new-manifestation affordance;
- `ONE_TO_MANY` may permit proposal but only after semantic/authority qualification;
- many-to-one target preserves originating semantic anchor;
- ambiguous existing-target mapping remains `UNKNOWN/AMBIGUOUS`, not auto-selected;
- target enumeration blocked by disclosure does not imply zero targets.

`Vertical + != clone`.
`Plus visible != new target admissible`.

### 5. CriticalStateMarker — multiplicity/conformance critical states

A must test representation floors for cardinality/mapping conditions in addition to prior critical states.

Candidate additions:

```text
AMBIGUOUS_MAPPING
UNDER_REALIZED
OVER_REALIZED
PARTIALLY_OBSERVED
OBSERVATION_STALE
OBSERVATION_UNKNOWN
CARDINALITY_CONFLICT
EXPECTED_BUT_MISSING
```

These remain qualified representation states/markers, not canonical business state owners.

A count match cannot silently render conformance success when readiness/effectiveness/currentness is partial or unknown.

## Performance refinement

Packet 03 workload evidence must now distinguish three independent scale dimensions:

```text
logicalObjectCount
manifestationOrObservedInstanceCount
renderedPrimitiveCount
```

A/B must reason about at least 1x and 10x manifestation fan-out without requiring every manifestation to be simultaneously materialized in the scene.

Required invariants:

- aggregation/clustering does not merge semantic identities;
- label virtualization does not remove selected/focused/critical representations;
- re-instancing cannot mutate semantic selection;
- picking identity remains adapter-local;
- hidden membership/counts obey disclosure constraints;
- performance degradation uses aggregation, not semantic omission.

No renderer benchmark harness is authorized in Packet 03; a provider-neutral benchmark-harness packet remains a later candidate after the projection contracts close.

## Accessibility refinement

A/B must preserve non-3D equivalents for multiplicity:

- manifestations list/table grouped by semantic anchor;
- keyboard-accessible aggregate expansion;
- `Show manifestations…` / equivalent command semantics;
- textual cardinality/currentness/conformance explanation;
- ambiguous mapping explanation without forced choice;
- non-color distinction for aggregate/ambiguous/partial/unknown conditions.

Tiny shaft stops or ports remain enhancement affordances, never the only path.

## Componentes impact

Do not promote new higher-level components yet. Extend Packet 03 scenarios with:

- one semantic anchor -> three manifestations -> select one -> switch 3D/list -> anchor preserved;
- many semantic contributors -> one manifestation -> originating selection context preserved;
- aggregate selected -> expand -> no implicit first-member selection;
- ambiguous observed mapping -> no auto-choice;
- expected 3 / observed 3 / effective 2 -> never false MATCHED;
- vertical `+` with max cardinality already satisfied -> explicit ineligibility;
- target enumeration undisclosed -> never infer zero;
- re-instancing changes RenderInstanceId -> selection unchanged;
- 10x manifestation fan-out -> aggregation preserves critical states.

The :40 candidate inventory (`ManifestationAggregate`, `CardinalityIndicator`, `ShaftBranch`, etc.) remains research input for later promotion, not accepted Packet 03 component scope.

## Deferred successor — module internal anatomy remains queued

The module-internal fractal/radial hypothesis remains valid and deferred:

```text
Core / capability intent
Contracts / ports
Services
Workflow
Data flow
Policies / authority
Providers / bindings
Adapters / drivers
Runtime / deployment hooks
Observability / evidence
```

Preserve:

- `Global floor != module-internal layer`;
- `Module internal face != new ModuleIdentity`;
- `Internal radial position != ownership/authority`;
- internal anatomy may project onto multiple global floors;
- recursive visualization must preserve identity rather than clone it.

Do not create the anatomy packet until Packet 03 A/B evidence closes projection identity/selection/cardinality fundamentals.

## Exact handoff — Construction A

Construction A must execute the existing Packet 03 five-object scope now, incorporating both the 11:50 identity/floor refinements and this 12:50 cardinality refinement:

1. `ProjectionIdentityRef`;
2. `SelectionBridge`;
3. `QualifiedRelationPort`;
4. `FloorManifestationPort`;
5. `CriticalStateMarker`.

A must return a fresh bounded artifact with state dimensions, transitions, failure/recovery, accessibility, performance, `composedOf/usedBy`, Componentes scenarios and unresolved semantic decisions.

A must not widen into ModuleNode, ModuleWorkbox, module internal anatomy, full Door/Counter/Corridor/Handoff/Gate family, Tool, Workspace or complete Canvas.

## Exact handoff — Construction B

After fresh A output, B remains authorized only for:

1. `ModuleProjectionTile`;
2. `ProjectionHandoff`.

B must add multiplicity scenarios to those patterns: aggregate selection, one-to-many manifestation, many-to-one target, ambiguity, re-instancing, disclosure-bounded aggregate and semantic-zoom substitution.

B must not promote complete Main Canvas yet.

## Closure criteria / proof obligations

Packet 03 still closes only when the five named lower/middle contracts exist and B has exercised the two bounded patterns. Added closure obligations this round:

- cardinality is relation-qualified, never geometrically inferred;
- identity is not strengthened/weakened by multiplicity;
- aggregate selection is distinct from member selection;
- semantic anchor survives manifestation pivot and projection handoff;
- one-to-many and many-to-one relations preserve provenance/context;
- ambiguous mapping remains explicit;
- hidden target membership is not inferred from absence;
- count equality never substitutes for multidimensional conformance;
- re-instancing does not alter semantic identity/selection;
- LOD/aggregation preserve critical cardinality/mapping uncertainty;
- vertical `+` respects relation cardinality and existing targets.

## Blocker and maturity

Current blocker:

`BLOCKER-G4-FE-PL-004 = Packet 03 Construction A materialization absent`.

No semantic contradiction was found. The blocker is sequencing-only and the recurring lane remains useful.

Maturity:

`PACKET_03_ACTIVE / IDENTITY+CARDINALITY_REFINED / A_REQUIRED / B_WAITING`.

Packet 04 must not be created until fresh A/B evidence closes Packet 03 or identifies a bounded lower-level repair.