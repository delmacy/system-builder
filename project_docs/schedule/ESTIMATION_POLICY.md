# Estimation Policy

Estimate only after a WP is decomposed enough that inputs, outputs, acceptance and dependencies are known.

Record implementation effort separately from review/test/integration overhead. Early estimates are ranges; actuals update future estimates. Do not convert uncertainty into fake precision.

For agent-executed work, track model/tool class, retries, review burden and CI failures as estimation data because cheap generation can shift cost into validation/rework.
