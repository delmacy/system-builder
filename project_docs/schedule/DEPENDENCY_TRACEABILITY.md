# Dependency Traceability

Every blocking edge should be traceable from consumer need to producer output and evidence gate.

Future task record chain: Requirement -> WBS -> Work Package -> Task -> Dependency Gate -> Milestone/Increment -> Sprint -> PR/Commit -> Evidence.

This allows the planner to explain why work is blocked and what exact artifact will unblock it.
