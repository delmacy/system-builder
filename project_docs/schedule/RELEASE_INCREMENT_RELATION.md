# Release and Increment Relation

Not every increment must be externally released. An increment must be integrated and demonstrable; a release is a governed artifact promoted through the release/deploy pipeline.

Early project increments may exist only as validated internal slices. Once Release/Deploy capabilities mature, selected increments can become preview/production releases.

This prevents schedule pressure from forcing immature internal increments into production merely to satisfy a sprint boundary.
