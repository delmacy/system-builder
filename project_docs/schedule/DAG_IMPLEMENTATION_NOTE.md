# DAG Implementation Note

The current documents define the DAG semantics and coarse network. The executable DAG should later be stored as version-controlled YAML/JSON generated or validated from WBS Dictionary entries, with a CI validator for missing nodes, cycles and unsatisfied gate schemas.

Human Markdown remains explanatory; machine data becomes the scheduler source once implemented.
