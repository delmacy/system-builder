# Roadmap Outputs

This stage outputs a whole-project capability DAG, milestone graph, logical waves, dependency/gate semantics, rolling-wave sprint policy, review/replanning policy, critical-path method, capacity/parallelism rules, templates for sprint/machine DAG/schedule baseline, and gates for the next detailed planning horizon.

It deliberately does not output arbitrary numbered sprints before activity estimates exist.
