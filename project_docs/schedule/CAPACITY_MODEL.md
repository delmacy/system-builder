# Capacity Model

Agent throughput is not the same as project throughput. Planning capacity includes implementation, review, CI/test availability, integration effort and required human approvals.

The scheduler limits active work using the narrowest relevant capacity. This becomes especially important before enabling multiple parallel executors.
