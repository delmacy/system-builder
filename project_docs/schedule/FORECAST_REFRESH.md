# Forecast Refresh

After each sprint: ingest actual effort, validation results, new blockers and unlocked gates; recompute READY set; adjust near-horizon estimates and candidate ordering.

After each system review: re-evaluate cross-module dependencies, technical debt, critical path and milestone confidence; regenerate the broader forecast package.

Completed sprint history remains immutable except for annotations/corrective links.
