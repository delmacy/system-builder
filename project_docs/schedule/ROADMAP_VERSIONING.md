# Roadmap Versioning

Version the coarse roadmap when milestone/capability dependency architecture materially changes. Version the schedule baseline when near-horizon estimates/order/critical path are rebaselined. Sprint artifacts retain immutable identifiers after instantiation.

All versions reference the applicable Scope Baseline and contract/spec revisions.
