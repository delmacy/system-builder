# No False Dependencies

Do not create a dependency because one item was numbered or scheduled earlier. Only technical, data, contract, runtime, validation, regulatory or deliberate resource constraints justify an edge.

If work is sequential only because the team currently has one executor, model that as capacity, not REQUIRES. This keeps the DAG valid when more agents/resources become available.
