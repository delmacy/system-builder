# Schedule Baseline Template

Created only after first-horizon task estimates exist.

Record: baseline version/date; target milestone/increment; task DAG snapshot; estimates and assumptions; critical path; float; capacity/WIP limits; forecast sprint horizon; review cycle; key risks; change-control reference.

Actual execution is compared against this baseline for learning/control. The baseline can be revised through documented schedule/change governance rather than silently overwritten.
