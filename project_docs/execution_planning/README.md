# Execution Planning — First Horizon

This folder refines the approved scope/DAG into the first executable planning horizon. It does not replace `docs/current/TASK_LEDGER.json` or existing task specs.
