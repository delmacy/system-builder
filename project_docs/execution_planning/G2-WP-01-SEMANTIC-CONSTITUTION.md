# G2-WP-01 — Semantic Constitution & Federated Revision Base

Status: PLANNING COMPLETE / CONSTRUCTION A MATERIALIZED / NOT EXECUTED
Date: 2026-09-07
Generation: Generation 2 — Capability Architecture & Symbiotic Platform Engineering
Fresh-main base: `d8760c7f08757bb164a758ae0c3f0a4a1752464b`
Planning authority branch: `research/g2-capability-pipeline`
Planning authority revalidated head: `2ef10187d691666b45cba5978671570f0ff90c2a`
WBS authority: `G2-WBS-01 — Semantic substrate, revision and graph constitution`

## Package goal
Introduce the minimum reusable, owner-preserving Generation 2 semantic substrate needed by later WBS nodes while preserving all current domain owners and existing G1 contracts. The package establishes stable semantic identity, immutable revision qualification, temporal/currentness coordinates, typed graph relations, provenance/evidence references and bounded federation/locality semantics without becoming a universal domain model, policy engine, provider facade, workflow owner or mutable source of truth.

## Fresh-main reconciliation
`main` remains at the exact Planning B evidence anchor used by Generation 2 current-state reconciliation. M19 is canonically CLOSED / PRE-ALPHA. Existing provider-neutral artifact identity/provenance, process/factory lineage, provider-aware catalog resolution and fail-closed runtime authority are preserved as strong predecessors. No current subsystem is selected for replacement.

The current repository does not yet expose the full G2 universal semantic substrate. In particular, no reusable owner-qualified `CanonicalSemanticIdentityRef`, sparse `RevisionVector`, general currentness qualification, typed semantic graph or federated/locality contract exists. This package therefore evolves additively and does not reinterpret bounded G1 contracts as if they already carried G2 semantics.

## Package boundary decision
Construction A may introduce an additive public structural contract family at `packages/contracts/semantic-substrate/**`. This is an L3 contract surface implementing the already-decided G2 architecture; it is not a new runtime module, semantic owner, storage topology, service, bounded context or L4 topology change.

The contract family may define only portable structural identities and qualification shapes. Domain predicates and lifecycles remain with their canonical owners. If implementation requires a new module/service boundary, reverse dependency, runtime topology, universal evaluator/policy owner, destructive contract replacement or another L4 change, Construction must stop for ADR/change control.

## Typed predecessor/readiness gate
- M19 / P19 package: CLOSED / PRE-ALPHA.
- Generation 2 Research/Synthesis/Planning A-E: CLOSED / PASS.
- Architecture Reconciliation: CLOSED / PASS.
- WBS Decomposition and typed DAG: CLOSED / PASS.
- `G2-WBS-01`: Layer L0 constitutional entry node with no predecessor WBS node.
- Worker handoff: CLOSED / PASS; user explicitly authorized execution of `G2-WP-01..13` on 2026-09-07.

## Forecast sprint topology
### Planning — `G2-WP01-PLANNING-MATERIALIZATION-01`
Reconcile fresh `main` against the G2 planning chain, select the additive contract boundary, forecast the package and materialize only Construction A.

### Construction A — `G2-SEMANTIC-CONTRACT-FOUNDATION-01` — COMMITTED / MATERIALIZED / NOT EXECUTED
Establish the portable semantic identity, revision, temporal/currentness, typed graph and bounded federation/locality contract foundation plus integrated product proof. No domain consumer migration is authorized in this Sprint.

### Construction B — `G2-SEMANTIC-CONSUMER-COEXISTENCE-01` — FORECAST
After Construction A integrates and fresh `main` is revalidated, consume the substrate through a small set of existing domain-scoped predecessor contracts/proofs without rewriting their historical identities or binary bounded evidence semantics. Establish explicit coexistence and directional references rather than big-bang normalization.

### Construction C — `G2-SEMANTIC-HARDENING-01` — OPTIONAL / FORECAST
Promote only after Construction B integration if fresh evidence proves a bounded remaining increment is necessary for the Package Goal, such as cross-owner graph transformation/federation hardening or residual compatibility proof. It must not exist merely to satisfy a target Sprint count.

### Package Integration & Review — FORECAST
Regress the complete WP-01 outcome for owner leakage, revision/currentness drift, provider/external identity collapse, federation strengthening, backward compatibility, architecture fitness, proof coverage and technical debt.

### Documentation & Closure — FORECAST
Reconcile repository memory, package evidence, WBS readiness and successor eligibility. No new product behavior.

## Construction A growing proof
Create two or more semantic subjects owned by different capability identifiers; prove stable owner-qualified identity, immutable definition/revision/occurrence distinction, sparse revision qualification, explicit temporal/currentness scope, typed directional relations and bounded local/federated references. Then prove adversarially that provider/external identity reuse, revision substitution, owner mismatch, unknown relation kinds, stale/unqualified currentness and federation inputs cannot silently strengthen or rewrite canonical truth.

## Package growing proof
`existing domain identity/provenance -> owner-qualified semantic reference -> immutable revision/currentness qualification -> typed relation -> local/federated reference -> existing domain consumer coexistence -> exact evidence/proof -> package reconciliation`.

## Constitutional invariants
- `CanonicalSemanticIdentity != Provider/External/RuntimeRealizationIdentity`.
- semantic owner is explicit and cannot be inferred from equal labels/values.
- producing revisions are immutable historical facts; current qualification does not rewrite history.
- provenance != truth != currentness != authority.
- graph transforms and federation are owner-preserving and non-strengthening.
- Fleet/global projection != Station/local truth.
- `PARTIAL` and `UNKNOWN` remain first-class where applicable; no scalar success can erase missing dimensions.
- no universal domain entity model, policy evaluator, authority broker, provider facade or orchestration engine is introduced.
- no generic direct physical actuation authority.
- Product Proof remains distinct from Production Readiness Coverage.

## Explicit non-goals
Elicitation Knowledge Base implementation; Identity/Auth/Authz business semantics; provider support-vector implementation; workflow execution/effects; data/schema migration; storage; UI/AGWS; deployment topology; observability; commercial/FinOps; Architecture Reconciliation runtime capability; migration of all existing contracts; carried TD-P13-01..04; unrelated research findings.

## Package gates
- Only Construction A is committed by this Planning Sprint.
- Construction B remains FORECAST until A is reviewed/integrated and fresh `main` is reconstructed.
- Construction C is optional and requires evidence after B.
- Each Construction Sprint requires one branch, committed TASKs, authoritative per-TASK commits, repository-wide verification, exact-head CI and Sprint Review before integration.
- No undeclared L4, destructive migration, security/governance weakening or forbidden-path requirement may be inferred from this package authorization.

## Completion definition
WP-01 closes only when the repository has a small reusable semantic substrate that can be consumed directionally by multiple owners without semantic leakage, preserves historical revisions/currentness/locality, keeps external/provider identities distinct, resists graph/federation strengthening, coexists with current G1 contracts and has objective Product Proof plus package-level reconciliation evidence.