# Generation 2 — Representative Coverage Ledger

States: `NOT_REVIEWED`, `PARTIAL`, `DEEP`, `N_A`, `LOW_VALUE`. Historical coverage remains authoritative in prior dossiers/state history; this compact ledger records current/recent revisits without superseding it.

## Revisit cycle 2–4
All 25 active capabilities completed cycles 2, 3 and 4. Every pass produced material findings; no capability was SATURATED at cycle close. Historical representative detail remains authoritative in capability dossiers and prior ledger revisions.

## Revisit cycle 5
All prior cycle-5 coverage through Authorization remains authoritative in capability dossiers and prior ledger revisions.

### Data / Schema / Migrations — revisit 4
Confluent Schema Registry compatibility/evolution/versioning: `DEEP`; Debezium incremental snapshot/checkpoint/schema-change semantics: `DEEP`; PostgreSQL MVCC/transactional-DDL boundary: `PARTIAL`; CockroachDB online schema-change job realization: `PARTIAL`; Google Cloud Spanner online schema update: `PARTIAL`; Prisma expand/contract migration pattern: `PARTIAL`. Findings `G2-FINDING-DSM-29..36`; material new findings; consecutive-no-material = 0; NOT SATURATED. Focus: canonical schema vs provider realization, directional compatibility windows, migration/backfill/CDC convergence evidence, concurrency/ordering requirements, destructive-change recovery, provider cutover/divergence reconciliation, PARTIAL/INCONCLUSIVE propagation, AGWS data-surface escalation and qualified local/offline migration closure.

## Historical authority
All omitted representative/capability cells remain authoritative in prior ledger revisions and capability dossiers.