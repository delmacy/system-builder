# Generation 2 — Edge-Case Index

Status: ACTIVE
Phase: RESEARCH_ADVERSARIAL_EDGE_CASE_SATURATION

No material scenarios are registered yet. Stable IDs will use `G2-EDGE-<CAPABILITY>-NNN` and cross-capability IDs `G2-XEDGE-<CLUSTER>-NNN`.

Every material entry must include owner(s), expected safe behavior, forbidden behavior, effect/failure disposition, authority boundary, evidence/currentness, recovery/reconciliation, blast radius, severity/misuse likelihood and proof obligation.

The index starts empty deliberately; initialization does not count as a full adversarial pass.