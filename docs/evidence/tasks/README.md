# Task Evidence

Task evidence records historical delivery facts and deterministic compatibility inputs. It does not grant current execution authority.

The bootstrap-era `docs/current/TASK_LEDGER.json` remains at its legacy path because deterministic AgentFactory I2 tests consume that exact fixture. Its contents are historical compatibility evidence only; use current repository memory, active Work Package/Sprint manifests and committed TASK specifications to select executable work.
