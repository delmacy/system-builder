# Execution Plans

Execution plans are durable milestone-level implementation records.

Target structure:

```text
docs/exec-plans/
  active/
  completed/
```

An active plan records objective, scope, dependencies, tasks, gates, decisions, risks and completion evidence. It is updated during execution rather than replaced by chat summaries.

Tasks remain small; execution plans preserve the larger thread.
