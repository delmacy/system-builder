# 000-base provenance record

Status: RECONSTRUCTION EVIDENCE
Date: 2026-09-26

This record separates the reconstructed contract statement from its corroborating repository evidence.

## Primary accepted authority

### `AGENTS.md`

The repository constitution explicitly records the durable invariants used by `000-base`:

- operational reality precedes technical abstraction;
- `BusinessRecipe != SystemDefinition`;
- `Builder != Runtime`;
- published runtime autonomy;
- compatibility before replacement;
- open/portable architecture;
- replaceable suite modules;
- explicit contracts between bounded contexts;
- durable decisions belong in repository memory.

These are accepted repository-level rules, not historical sprint status.

## Corroborating integrated evidence

Historical package/review material is used only to confirm that later implementation preserved the founding boundaries. Examples include integrated evidence describing:

- deterministic SystemDefinition -> catalog/assembly/compiler/release/deploy -> autonomous Runtime flows;
- ordinary generated Runtime operation without Builder availability;
- preservation of `BusinessRecipe != SystemDefinition` through downstream assembly/compiler work.

Such execution material does not independently create or date founding scope; it corroborates continuity of the reconstructed baseline.

## Exclusions

No Generation 3/4 research document is treated as founding authority. No open/draft PR is treated as accepted provenance. No current Station implementation detail is backdated into the base contract.

## Reconstruction confidence

The constitutional boundaries above have high confidence because they are explicit in current accepted repository authority and repeatedly preserved by integrated implementation evidence.

The exact original calendar date and original prose of the founding user request are not asserted here because the repository evidence inspected in this pass does not establish them with sufficient authority. Chronological addenda must follow the same conservative rule.
