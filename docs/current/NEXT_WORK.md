# Next Work

The task catalog is authoritative for status/dependencies; `npm run task:next` selects the first executable task.

1. Configure the recommended `main` ruleset in GitHub and require the `validate` check.
2. Configure OpenCode locally; no OpenCode configuration belongs in this repository task.
3. Run TASK-002 with the free model using only its generated Task Pack and the documented Git workflow.
4. Review, merge and close the synthetic handoff proof.
5. Continue TASK-003 through TASK-008 serially.

No product module, legacy source extraction or parallel workstream is ready before these dependencies complete.
