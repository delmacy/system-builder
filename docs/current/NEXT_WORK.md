# Next Work — G2-WP-02 Planning & Materialization

Generation 2 execution remains rolling-wave and dependency-safe.

## Closed predecessor
`G2-WP-01 — Semantic Constitution & Federated Revision Base` is canonically CLOSED. Documentation & Closure execution exact head `6b206a9a51924d0de08bd0cd2e2f0eedc94d1f94` passed Deterministic CI #1415, Heavy Product Tests #897 and Automation Handoff and merged through PR #558 as fresh main `3038b6afa3549af5f7b9716e8f579b364db28c3b`. Reviewed-head -> merge-main comparison has zero changed files.

## Next dependency-safe Work Package
The exact G2 Work Package design declares `G2-WP-02 — Elicitation Knowledge Base & System Understanding` with typed prerequisites only from `WP-01 [SEMANTIC_PREREQUISITE, REVISION_PREREQUISITE, EVIDENCE_PREREQUISITE]`. WP-01 closure satisfies that predecessor gate, so WP-02 is eligible for bounded Planning & Materialization.

Do not promote WP-03 or WP-04 as a side effect: both carry WP-02 prerequisites. `G2-WP-03..G2-WP-13` remain DESIGNED / NOT MATERIALIZED.

## Planning obligations
Materialize only the first coherent WP-02 Construction Sprint and its executive TASK chain. Preserve `QuestionDefinition != QuestionOccurrence`; distinct `Fact`, `Claim`, `Assumption`, `InferredCandidate`, `Decision`, `Requirement`, `Constraint`, `OpenQuestion`, `Conflict`, `Unknown`, `OutOfScope`, `Deferred`; `AI inference = candidate`; `observed behavior != intended process != approved canonical process`; explicit contradiction owner/evidence/decision routes; HIGH/CRITICAL gaps preventing false complete; dimensional rather than scalar sufficiency; capability-specific routing without duplicating semantic ownership; and currentness/provenance/locality qualification inherited from WP-01.

Planning must not conceal implementation, introduce a 29th canonical capability, absorb unrelated findings, or claim Production Readiness Coverage.
