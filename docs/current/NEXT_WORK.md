# Next Work — G2-WP-02 Planning & Materialization

Generation 2 execution remains rolling-wave and dependency-safe.

## Closed predecessor
`G2-WP-01 — Semantic Constitution & Federated Revision Base` is canonically CLOSED. Documentation & Closure exact head `6b206a9a51924d0de08bd0cd2e2f0eedc94d1f94` passed Deterministic CI #1415, Heavy Product Tests #897 and Automation Handoff and integrated through PR #558 as merge `3038b6afa3549af5f7b9716e8f579b364db28c3b`.

## Dependency-safe successor
Revalidation of `GENERATION_2_WBS_DEPENDENCY_GRAPH.md` and `GENERATION_2_WORK_PACKAGE_DESIGN.md` selects `G2-WP-02 — Elicitation Knowledge Base & System Understanding`. Its typed entry prerequisites are `WP-01 [SEMANTIC_PREREQUISITE, REVISION_PREREQUISITE, EVIDENCE_PREREQUISITE]`; those prerequisites are satisfied by WP-01 closure. WP-03 and WP-04 still consume WP-02 evidence/authority-discovery outputs and are not promoted by this reconciliation.

## Active gate
Materialize only the smallest coherent WP-02 Planning & Materialization Sprint from fresh `main`. Re-read repository policy, relevant WBS/package authority and existing knowledge/elicitation contracts before creating executive TASKs. Materialize only the first eligible Construction Sprint; later Construction, Package Review and Closure remain forecast until predecessor gates pass.

Preserve EKB as hybrid, versioned, auditable and cross-cutting infrastructure rather than a static questionnaire or canonical capability. Preserve `QuestionDefinition != QuestionOccurrence`; keep `Fact`, `Claim`, `Assumption`, `InferredCandidate`, `Decision`, `Requirement`, `Constraint`, `OpenQuestion`, `Conflict`, `Unknown`, `OutOfScope`, `Deferred` distinct; `AI inference = candidate`; observed behavior != intended process != approved canonical process; HIGH/CRITICAL gaps prevent false-complete; coverage remains dimensional rather than a scalar quality score.

Do not absorb TD-P13-01..04, unrelated findings, provider mechanics, workflow behavior or later-WP implementation into WP-02 planning.